{
  "name": "leetcodetotodoist",
  "version": "0.1.1",
  "description": "Chrome extension to schedule LeetCode problems into Todoist.",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && node scripts/copy-static.js",
    "watch": "tsc --watch",
    "release": "node scripts/prepare-release.js"
  },
  "keywords": [
    "chrome-extension",
    "leetcode",
    "todoist"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.273",
    "typescript": "^5.4.0"
  }
}
